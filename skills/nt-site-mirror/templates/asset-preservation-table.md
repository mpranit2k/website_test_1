# Asset Preservation Table

> Required before implementing any section affected by a major motion, media, audio, WebGL, video, runtime, or route-specific asset system.
> Status must be exactly one of: `Original` | `Local Copy` | `Embed` | `Kept External` | `User-Supplied Replacement` | `Recreated` | `Recreated From Observation` | `Approximated` | `Blocked`
> Reminder: paid fonts, Vimeo, Mux, YouTube, provider-streamed and protected assets are never automatically downloadable. `Local Copy` requires authorization + license + technical permission.

| Asset | Source (URL / code path) | Status | Strategy & why alternatives were rejected | Fidelity impact |
| --- | --- | --- | --- | --- |
| | | | | |

## Approvals needed

> Any row with `Approximated` requires user approval BEFORE implementation (core Approval Gate). List pending approvals:

-
